In this assignment, we do programming on complex dataset, apply combiner to see its effect on transfer rate by binary data. TopNList is not easy to be implemented, however, after discussion about the task, we decided to use data structure to hold the key and value. It also needs to match the movie index with its real name. Second problem help us review what does actually combiner do and effect in MapReduce jobs
